# Password Generator

A Python project that generates strong random passwords using letters, numbers, and symbols.

## Features
- Random password generation
- Uses letters, numbers, and special symbols
- Shuffle support for randomness
- Minimum password length validation

## Technologies Used
- Python
- random module

## How to Run
Run the Python file using:

python password_generator.py
